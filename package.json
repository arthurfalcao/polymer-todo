{
  "name": "polymer-todo",
  "scripts": {
    "start": "concurrently 'npm run serve' 'npm run watch'",
    "serve": "polymer serve",
    "watch": "browser-sync start --proxy localhost:8081 --open \"src/components\" --files \"**/*.js\"",
    "build": "polymer build"
  },
  "dependencies": {
    "@polymer/polymer": "^3.4.1",
    "browser-sync": "^2.26.13",
    "concurrently": "^5.3.0"
  },
  "devDependencies": {
    "@polymer/test-fixture": "^4.0.2",
    "@webcomponents/webcomponentsjs": "^2.5.0",
    "chai": "^4.2.0",
    "mocha": "^8.2.1",
    "wct-mocha": "^1.1.0"
  }
}
